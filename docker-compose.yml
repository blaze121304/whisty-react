version: '3.8'

services:
  whisty-react:
    image: whisty-react:latest
    container_name: whisty-react
    ports:
      - "10007:10007"
    restart: unless-stopped
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_API_BASE_URL: "http://host.docker.internal:10006/api"

